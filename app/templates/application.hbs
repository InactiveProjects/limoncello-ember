<nav>
    <div class="menu">
          {{#link-to 'index' class=""}}Home{{/link-to}}
          {{#link-to 'authors'}}Authors{{/link-to}}
          {{#link-to 'sites'}}Sites{{/link-to}}
          {{#link-to 'users'}}Users{{/link-to}}
          {{#link-to 'comments'}}Comments (broken: no relationship){{/link-to}}
          {{#link-to 'posts'}}Posts (broken: no relationships){{/link-to}}
      {{#if session.isAuthenticated}}
            <button {{ action "invalidateSession" }} class="error">Logout</button>
        {{/if}}
    </div>
</nav>
<main>
  {{outlet}}
</main>